[project]
name = "tokyo-sticker-db"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.1",
    "torchvision>=0.24.1",
]

[tool.uv.workspace]
members = [
    "backend",
]
